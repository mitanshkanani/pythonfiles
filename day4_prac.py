import random 
# if we want to import a file we also use this import keyword but it should be in same folder
import hello
print(hello.hi)
#for generating random number between a to b we use randint(a,b)
c=random.randint(100,200)
print(c)

random_integer=random.uniform(1.00,2.00)
print(random_integer)
